# Configuración del servidor Kyrbi
# Copia este archivo como .env y completa los valores

# Puerto del servidor (opcional, por defecto 3000)
PORT=3000

# API Key de OpenAI
# Obtén tu clave en: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-tu-api-key-aqui

# Modelo de OpenAI a usar
# Opciones: gpt-3.5-turbo (más económico) o gpt-4 (más potente pero más caro)
OPENAI_MODEL=gpt-3.5-turbo

# Entorno (development o production)
NODE_ENV=development

# URL pública del backend (para enlaces en correos)
PUBLIC_BACKEND_URL=http://localhost:3000

# URL pública del frontend (opcional)
PUBLIC_FRONTEND_URL=http://localhost:5500

# Configuración SMTP para envío de correos
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=usuario@example.com
SMTP_PASS=contraseña_super_segura
MAIL_FROM=\"Kyrbi\" <no-reply@kyrbi.local>

# Google reCAPTCHA v3
RECAPTCHA_SECRET=tu_clave_secreta_recaptcha
